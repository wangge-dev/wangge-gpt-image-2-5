# EC48 · 小家电面板状态与配件对应

[全部提示词](../README.md) · [首页](../../README.md)

**分类：小家电 · 本库原创设计**

将真实控制面板、指定工作状态和配件清单绑定，避免虚构按钮、程序或功能。

## 准备材料与变量

图1：主机完整照；图2：断电状态面板高清照；图3：指定状态的真实面板照片；图4：全部随附配件同框照；图5：说明书对应页面。

将下表中的值按实际任务填写，然后复制正文。示例值用于说明填写方式，不是已核实的商品事实。

| 变量 | 填写示例或说明 |
| --- | --- |
| `{{商品身份}}` | 台式破壁机 |
| `{{指定状态}}` | 预约时间显示08:00 |
| `{{配件清单}}` | 主杯1个、杯盖1个、量杯1个、清洁刷1把 |
| `{{标题}}` | 面板与配件 |

## 拿来就用的具体示例

准备主机、断电面板、指定真实状态、全部配件和说明书对应页；指定状态必须在实机中存在。

按示例准备对应素材后，直接复制下段；其他商品使用后面的变量模板。

```text
依据图1至图5制作4:5台式破壁机“面板与配件”图。上半部保持图1主机、杯体、底座和品牌位置；面板按图2保留真实按键，只将显示区按图3呈现“08:00”，其余灯位不变。下半部只展示主杯1个、杯盖1个、量杯1个、清洁刷1把，各一次。不要增加程序、第二台主机、食材、饮品、功率、转速、静音或效果结论。只输出一张图。
```


## 完整提示词

```text
依据图1至图5制作4:5{{商品身份}}“{{标题}}”说明图。上半部保持图1主机外形、杯体、底座、品牌位置和接口；右侧面板特写以图2定义按键数量、名称和位置，只把显示区按图3改为{{指定状态}}，其他指示灯保持图3，不新增程序。下半部严格按图4展示{{配件清单}}，每件一次，并用细引线标出名称；图5只用于核对面板和配件，不生成说明书未列出的功能。不要添加第二台主机、食材、成品饮品、蒸汽或人物，不写功率、容量、转速、静音、预约能力以外的功能、安全认证或效果结论。只输出一张图。
```

## 参数设置

建议画布：`1536x1920`；模型选择及格式见[模型说明](../../guides/models.md)。网页版可按相同比例描述画布。

界面截图和说明书只证明对应状态与功能，不应由灯位或按钮外观推断额外程序和性能。


修改前，将修改指令中的双花括号内容按实际问题填写；每次只处理一个位置。

## 接着修改

上传上一轮结果；涉及商品或人物时，同时保留原始参考图。

```text
只修正{{面板区或配件区}}的{{按键、显示、灯位、数量或文字问题}}，按图{{参考图号}}恢复；主机和其他区域不变。
```

## 来源

- [官方 Sunburst 模型说明 · 编辑能力依据](https://developers.openai.com/api/docs/models/gpt-image-2.5-sunburst)

整理日期：2026-09-09。上游许可及第三方素材边界见[署名说明](../../ATTRIBUTIONS.md)。
