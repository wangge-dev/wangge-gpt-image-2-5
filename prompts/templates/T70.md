# T70 · 按压泵头锁止方向与回弹高度对照

[全部提示词](../README.md) · [首页](../../README.md)

**分类：Products & E-commerce · 本库原创设计**

用同一泵头的锁止、人工确认解锁和真实回弹状态约束旋转方向、喷嘴朝向与可见高度，不把外观变化写成功能或出液证明。

## 准备材料与变量

图1：同一泵头锁止状态；图2：颈圈方向标记微距；图3：人工确认解锁状态；图4：人工确认完全回弹状态；图5：编号、方向与人工状态表。

将下表中的值按实际任务填写，然后复制正文。示例值用于说明填写方式，不是已核实的商品事实。

| 变量 | 填写示例或说明 |
| --- | --- |
| `{{商品}}` | 洗手液按压泵瓶 |
| `{{标记}}` | 瓶身P1、泵头H1、颈圈R1、方向点M1 |
| `{{方向}}` | 按人工记录逆时针旋至M1对齐 |
| `{{状态}}` | S0锁止；S1已解锁；S2完全回弹，喷嘴朝正前 |
| `{{标题}}` | 旋开之后，泵头升到哪里 |

## 拿来就用的具体示例

三状态需来自同一编号泵头，方向、解锁和完全回弹结论由人工提供；外观升起不证明泵送量、密封或正常工作。

按示例准备对应素材后，直接复制下段；其他商品使用后面的变量模板。

```text
制作16:9“旋开之后，泵头升到哪里”。同一洗手液按压泵瓶依次展示S0锁止、S1按人工记录逆时针旋至M1对齐、S2完全回弹且喷嘴朝正前。保持P1瓶身、H1泵头、R1颈圈、标签、机位和比例；回弹高度与颈圈间隙只按实拍。不画内部弹簧、导管、按压或出液，不判断出液量、密封、耐久或安全。只输出一张图。
```


## 完整提示词

```text
依据图1至图5制作16:9“{{标题}}”三状态核对板。依次展示同一{{商品}}的{{状态}}，保留{{标记}}。S0完全按图1；S1只用一支短弧箭头表示“{{方向}}”，箭头避开泵头和标签；S2按图4保留真实泵头高度、颈圈间隙、弹簧杆可见范围与喷嘴方向。三栏保持瓶体、螺纹颈、泵头形状、标签、机位和比例一致，不把S1提前画成完全回弹，不镜像喷嘴，不补内部剖面、弹簧或导管。高度和状态只来自图5人工记录，不从画面计算。不要表现按压、出液、泡沫或使用动作，不推断单次出液量、密封、回弹力、耐久、兼容或卫生安全。只输出一张静态对照图。
```

## 参数设置

建议画布：`2048x1152`；模型选择及格式见[模型说明](../../guides/models.md)。网页版可按相同比例描述画布。

视觉对照不替代泵送测试、密封测试、剂量测量、材料兼容或卫生验收。


修改前，将修改指令中的双花括号内容按实际问题填写；每次只处理一个位置。

## 接着修改

上传上一轮结果；涉及商品或人物时，同时保留原始参考图。

```text
只修正{{S0、S1、S2、H1、R1或M1}}的{{方向、喷嘴、可见高度、间隙、编号或人工状态}}，按图{{参考图号}}恢复；其他状态不变。
```

## 来源

- [官方 GPT Image 2.5 提示词指南](https://developers.openai.com/api/docs/guides/image-prompting)

整理日期：2026-09-09。上游许可及第三方素材边界见[署名说明](../../ATTRIBUTIONS.md)。
